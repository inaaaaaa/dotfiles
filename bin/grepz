#!/bin/bash

function usage() {
    echo 'usage: grepz <word> <path>'
    exit 1
}

[ "$#" -ne 2 ] && usage
WORD=$1
PATH_=$2

grep --context=2 --color=always --ignore-case \
     --line-number --recursive $WORD $PATH_
