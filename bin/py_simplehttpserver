#!/bin/bash

major_version=$(python --version 2>&1 | grep -o '[0-9].[0-9].[0-9]' | cut -d'.' -f1)

case $major_version in
    2)
        python -m SimpleHTTPServer
        ;;
    3)
        python -m http.server
        ;;
    *)
        echo 'error'
        exit 1
        ;;
esac
